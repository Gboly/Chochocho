import { communitySlice } from "../../pages/community/communitySlice";

export const {
  openOutletOptions,
  closeOutletOptions,
  setOutletOption,
  ignoreSuggestedUser,
} = communitySlice.actions;
